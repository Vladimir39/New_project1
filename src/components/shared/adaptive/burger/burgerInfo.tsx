import { Button } from "@/components/ui";
import React, { FC } from "react";
import { BurgerModal } from "./burgerModal";
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

export const BurgerInfo: FC<Props> = ({ className }) => {
  return <div></div>;
};
